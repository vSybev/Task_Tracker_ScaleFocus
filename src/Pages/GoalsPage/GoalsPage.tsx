import { Typography, Stack } from '@mui/material';

export function GoalsPage() {
    return (
        <Stack spacing={1}>
            <Typography variant="h4">Goals</Typography>
            <Typography color="text.secondary">Coming next: goals CRUD + progress.</Typography>
        </Stack>
    );
}
